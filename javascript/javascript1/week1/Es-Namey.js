const firstWords = [OlaCabs, AddressHealth, Zomato, Paytm, FreshToHome];
const secondWords = [FreshMenu, Flyrobe, Myra, Shuttl, Dunzo];

let startupName = [firstWords[1], conct(secondWords[0])];

const randomNumber = Math.floor(Math.random() * 10) + 0;

console.log(startupName[0]);