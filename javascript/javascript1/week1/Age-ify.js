// Age calculator code

const yearOfBirth = 1987; // use const as birth year is fixed
let yearFuture = 2078; // use let as it can be assiged to different values
const age = (yearFuture - yearOfBirth);
console.log('You will be ' + age + ' years old in ' + yearFuture);